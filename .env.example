# Server-side only. Never prefix secrets with NEXT_PUBLIC_.
RESEND_API_KEY=

# Public contact addresses may be configured explicitly or use the safe defaults in code.
CONTACT_EMAIL_TO=contacto@vigiacommand.cl
CONTACT_EMAIL_FROM=VIGIA Contact <noreply@vigiacommand.cl>

# Optional lead persistence. Leave both empty to disable persistence.
# Do not commit real URLs or tokens.
KV_REST_API_URL=
KV_REST_API_TOKEN=
